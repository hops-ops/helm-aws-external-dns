apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: externaldns.helm.aws.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: helm.aws.hops.ops.com.ai/v1alpha1
    kind: ExternalDNS
  mode: Pipeline
  pipeline:
  - functionRef:
      name: hops-ops-helm-aws-external-dnsrender
    step: render
  - functionRef:
      name: crossplane-contrib-function-auto-ready
    step: crossplane-contrib-function-auto-ready
